{
  "name": "eagereyes-comment-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../function.zip . && cd .. && zip -r function.zip node_modules package.json"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.2",
    "@aws-sdk/client-ses": "^3.758.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.7.3"
  }
}
